name: Deploy to Tencent Cloud

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Execute Deployment Script
        run: |
          cd /var/www/ai-hub
          chmod +x deploy.sh
          ./deploy.sh
